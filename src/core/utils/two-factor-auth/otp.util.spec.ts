import { MatriculeGenerate } from '../matricule_generate/matricule_generate.util';

describe('MatriculeGenerate', () => {
  it('should be defined', () => {
    expect(new MatriculeGenerate()).toBeDefined();
  });
});
